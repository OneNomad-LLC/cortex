# Caddy reverse proxy — generated by ./bootstrap.sh from this template.
# Auto-TLS via Let's Encrypt; the only public surface for the platform.

__CORTEX_DOMAIN__ {
    encode zstd gzip
    reverse_proxy cortex:3100
}

__ACCESS_DOMAIN__ {
    encode zstd gzip
    reverse_proxy access-service:4400
}
